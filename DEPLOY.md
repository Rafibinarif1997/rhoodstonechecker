## Deploy

### Google AI Studio
Upload/import this source, keep the Vite structure, add the VITE_* environment variables, install dependencies and build.

### Vercel / Netlify
Build command: `npm run build`
Output directory: `dist`

### GitHub Pages
Use a Vite GitHub Pages deployment workflow rather than uploading only `index.html`, because this build uses React routes and environment variables.
